//Arrow function to calculate area of triangle

var areaOfTriangle = (base, height) => (base * height)/2

const base = 10
const height = 6

var areaOfTriangle = areaOfTriangle(base,height)

console.log("Area of Traiangle: " + areaOfTriangle)